<div class="page-header">
	<h1><i class="icon-user"> </i>Update User</h1>
</div>
<p>
	<%= simple_form_for([:admin,@user], :html => {:class => 'form-horizontal' }) do |f| %>
		<ul class="nav nav-tabs">
	    <li class="active"><a href="#parts" data-toggle="tab">Info</a></li>
	    <li><a href="#device" data-toggle="tab">Groups</a></li>
	  </ul>

		<div class="tab-content">
			<div class="tab-pane active" id="parts">
				<%= f.input :fullname %>
				<%= f.input :email, :disabled => true %>
			</div>
			<div class="tab-pane" id="device">
				<%= f.association :groups, :as => :check_boxes %>
			</div>
	  </div>

		<div class="form-actions">
			<%= f.button :submit, 'Save', :class => 'btn btn-primary'%>
			<%= link_to 'Cancel', admin_user_path(@user), :class => 'btn'%>
		</div>
	<% end %>
</p>
