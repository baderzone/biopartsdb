<div class="page-header">
	<h1><i class="icon-folder-open"> </i>Update User</h1>
</div>
<p>
	<%= simple_form_for([:admin,@group], :html => {:class => 'form-horizontal' }) do |f| %>
		<ul class="nav nav-tabs">
	    <li class="active"><a href="#parts" data-toggle="tab">Info</a></li>
	  </ul>

		<div class="tab-content">
			<div class="tab-pane active" id="parts">
				<%= f.input :name %>
				<%= f.input :description %>
			</div>
	  </div>

		<div class="form-actions">
			<%= f.button :submit, 'Save', :class => 'btn btn-primary'%>
			<%= link_to 'Cancel', admin_groups_path, :class => 'btn'%>
		</div>
	<% end %>
</p>
